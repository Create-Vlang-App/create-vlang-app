# CLI branding placeholders

Terminal UX uses plain ASCII (no emoji dependency):

- Banner: `create-vlang-app — scaffold V projects` (`brand_banner`)
- Success: `OK  Scaffolded <path>`
- Errors: `ERR <message>`

Future: optional color via `CVA_COLOR=auto|always|never` (not required for M2).
