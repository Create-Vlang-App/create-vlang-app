# Distribution setup

Primary: **VPM** (`v install create-vlang-app`).

Secondary (see Epic 8 issues):

- GitHub Release binaries
- AUR (`Create-Vlang-App/aur-package`)
- Homebrew (`Create-Vlang-App/homebrew-tap`)
- Docker (optional)

Secrets and one-time setup will be expanded when publish workflows land.
